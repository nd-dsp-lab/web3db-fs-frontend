; react-scripts@5 pins typescript@^4 while the Reown/WalletConnect wallet
; stack resolves its own deps; npm@7+ (Amplify, CI) rejects this as a peer
; conflict. Installing with legacy peer resolution is the accepted fix.
legacy-peer-deps=true
